
# Touchstone QA host
node_modules/
.qa/artifacts/
package-lock.json

# Secrets / trust material — regenerated by `make certs`, NEVER committed to a remote
certs/

# Local env (ports only; copy this from .env.example after clone)
.env
