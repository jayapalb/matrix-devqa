; touchstone is a file: dep — COPY it (not a symlink) so its @playwright/test peer
; resolves to this project's single copy. A symlink → two Playwright instances → "No tests found".
install-links=true
