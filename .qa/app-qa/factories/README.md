# Test-data factories (app-tier)

Functions that build valid domain objects for tests (e.g. `makeUser(overrides)`). Import them in your
suites — keeps tests readable and data realistic. Plain modules; no registration needed.
