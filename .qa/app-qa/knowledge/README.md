# Knowledge cards (app-tier)

Markdown "playbook cards" (recipe / technique / gotcha) specific to this app. The `/qa-learn` skill writes
here; `touchstone knowledge` reads them and surfaces relevant ones during runs.
