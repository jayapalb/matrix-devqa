# Dev-only MQTT broker config — anonymous access, single plaintext listener.
# The registry + device agents publish/subscribe presence + events here (conformance L2).
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
