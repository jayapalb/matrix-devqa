# External-service mocks (app-tier)

Drop `<name>.mjs` for a named server-side double, or configure `mockServer` / `externalMocks` in
qa.config. Auto-discovered by name. Keeps tests off real third parties. See `touchstone/docs/EXTENDING.md`.
