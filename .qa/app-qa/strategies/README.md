# Auth strategies (app-tier)

Drop `<name>.mjs` for a custom auth strategy beyond the built-in cookie/token/custom/storageState. Select
via qa.config `auth: { strategy: '<name>' }`. See `touchstone/docs/EXTENDING.md`.
