# Matrix Plus dev broker — anonymous, single listener. NOT for production
# (production uses per-client certs + TLS on the room broker; SOW §0.6).
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
